

const COMPANY_TYPE = {
    values: ['insurance company', 'tow truck company'],
    message: '{VALUE} is not a valid company type'
};

const VEHICLE_TYPE= {
    values: [null],
    message: '{VALUE} is not a valid company type'
};

module.exports = {
    COMPANY_TYPE,
    VEHICLE_TYPE
}