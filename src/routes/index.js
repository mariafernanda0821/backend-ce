const express = require('express');
const app = express();
//ejemplo
//app.use('ruta', require('./ruta'));

// app.use('/api/armband', [
//     require('../middlewares/auth'),
//     require('../middlewares/role').isTourGuide
// ], require('./armband'));
